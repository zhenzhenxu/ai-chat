{
  "servers": {
    "github": {
      "command": "npx",
      "args": [
        "--yes",
        "@modelcontextprotocol/server-github",
        "--repositories",
        "zhenzhenxu/ai-chat"
      ],
      "env": {
        "GITHUB_TOKEN": "${env:GITHUB_TOKEN}"
      }
    }
  }
}
